import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplenotesorganizer.app',
  appName: 'Simple Notes Organizer',
  webDir: 'dist'
};

export default config;
